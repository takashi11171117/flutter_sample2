name: flutter_sample2
description: A new Flutter application.
version: 1.0.0+1
environment:
  sdk: ">=2.12.0 <3.0.0"
dependencies:
  cupertino_icons: ^1.0.2
  flutter:
    sdk: flutter
  flutter_hooks:
  flutter_localizations:
    sdk: flutter
  freezed_annotation:
  ftpconnect: ^1.0.0
  hive: ^2.0.4
  hive_flutter: ^1.1.0
  hooks_riverpod:
  intl: 0.17.0
  path_provider: 2.0.2
dev_dependencies:
  build_runner:
  flutter_test:
    sdk: flutter
  freezed:
  hive_generator: ^1.1.1
  pedantic_mono:
flutter:
  generate: true
  uses-material-design: true
  assets:
    - assets/images/

